apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: {{ include "maintenance-dashboard.fullname" . }}
  labels:
    release: {{ .Release.Name }}-kube-prometheus-stack
spec:
  endpoints:
  - path: /metrics
    port: http
  jobLabel: {{ include "maintenance-dashboard.fullname" . }}
  selector:
    matchLabels:
      app: maintenance-dashboard
