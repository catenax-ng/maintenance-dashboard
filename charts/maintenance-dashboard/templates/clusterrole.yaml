apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole

metadata:
  name: {{ include "maintenance-dashboard.serviceAccountName" . }}-cluster-role

rules:
  - apiGroups: [""]
    resources: ["deployments", "nodes", "statefulsets", "daemonsets"]
    verbs: ["list", "get"]