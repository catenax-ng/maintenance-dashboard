apiVersion: v1
kind: Service
metadata:
  name: {{ include "maintenance-dasboard-app.fullname" . }}
  labels:
    {{- include "maintenance-dasboard-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "maintenance-dasboard-app.selectorLabels" . | nindent 4 }}
