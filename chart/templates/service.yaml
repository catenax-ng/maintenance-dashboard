apiVersion: v1
kind: Service
metadata:
  name: {{ include "maintenance-dashboard-app.fullname" . }}
  labels:
    {{- include "maintenance-dashboard-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "maintenance-dashboard-app.selectorLabels" . | nindent 4 }}
