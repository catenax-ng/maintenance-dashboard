apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "maintenance-dasboard-app.fullname" . }}-test-connection"
  labels:
    {{- include "maintenance-dasboard-app.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "maintenance-dasboard-app.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
